#deep-1

meta {
	version = "2017-10-10";
	description = "Project File for junit tests on host for CFG, SSA, cgPPC";
}

project junitTestProject {
	libpath = "../ch.ntb.inf.deep.trglib";
	boardtype = ntbMpc555HB;
	ostype = sts_ppc;

	rootclasses = "";
}
